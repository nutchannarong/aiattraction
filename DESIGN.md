# ไทยไหนดี — Design System

เอกสารนี้สรุประบบดีไซน์จากต้นแบบ `ThaiNhaiDee-offline_1.html` (ต้นแบบแอปวางแผนทริปแบบออฟไลน์ ไฟล์เดียว) เพื่อใช้เป็นแหล่งอ้างอิงเดียวเวลาสร้างหน้าหรือ component ใหม่ใน Next.js
- **เรื่องโค้ด:** naming, Tailwind, Supabase ดูที่ [STYLE.md](STYLE.md)
- **เรื่องหน้าตาและพฤติกรรม UI:** ใช้เอกสารนี้

ไฟล์ภาพจากต้นแบบอยู่ใน `brand/`:

| ไฟล์ | ใช้ทำอะไร |
| --- | --- |
| `brand/logo-source.webp` | โลโก้เต็ม (มาสคอตพร้อมคำว่า "ไทยไหนดี") ใช้สร้างไอคอนทั้งหมดผ่าน `scripts/generate-brand-assets.mjs` |
| `brand/mascot-driving.png` | มาสคอตจับพวงมาลัย ใช้เป็น brand mark บน header (ต้นแบบแสดงที่ 38×36px) |
| `brand/hero-collage.jpg` | ภาพ hero แบบคอลลาจตัดแปะ (หญิงนักเดินทางบนรถมินิสีส้ม วัด เกาะ เสาชิงช้า) |

---

## 1. แนวคิดการออกแบบ

1. **สมุดทริปแปะสติกเกอร์ (Scrapbook / Neo-brutalism)**
   - การ์ดทุกใบมีขอบหมึกดำ 2px และเงาแข็งไม่เบลอ ให้ความรู้สึกเหมือนสติกเกอร์แปะบนกระดาษ
   - มีเทปกาวสีส้มแปะเฉียง และไฮไลต์แบบปากกาเน้นข้อความใต้คำสำคัญ
   - พื้นหลังเป็นกระดาษลายจุด
2. **ซื่อตรงกับข้อมูล**
   - บอกเสมอว่าข้อมูลส่วนไหนมาจาก ททท. และส่วนไหนระบบคำนวณเอง
   - ถ้าไม่มีข้อมูลต้องบอกว่าไม่มี และห้ามแต่งขึ้นมาเอง เช่น "ร้านอาหาร คาเฟ่ ที่พัก ปั๊มน้ำมัน ไม่มีในชุดข้อมูล ททท."
3. **วางแผนตามคนที่ไปด้วยจริง** ถามว่าไปกับใคร อายุช่วงไหน และขับรถอะไร แล้วปรับแผนให้เหมาะ แทนการแนะนำแบบกว้าง ๆ
4. **ภาษาไทยก่อน พูดเหมือนคน** ใช้คำถามสั้นแบบบทสนทนา เช่น "จะไปไหน เมื่อไร" หรือ "เดินทางไปกับใคร" แทนคำสั่งทางการ
5. **ตัวเลขต้องอ่านง่ายและเทียบกันได้** เวลา ระยะทาง และค่าใช้จ่ายใช้ฟอนต์ monospace และตัวเลขความกว้างเท่ากัน (`tabular-nums`)

---

## 2. Design tokens

### 2.1 สี

| Token (ต้นแบบ) | บทบาท | Light | Dark |
| --- | --- | --- | --- |
| `--paper` | พื้นหลังหน้า (กระดาษ) | `#FDF8F4` | `#101114` |
| `--card` | พื้นการ์ด / input | `#FFFFFF` | `#191B20` |
| `--card2` | พื้นรอง (หัวการ์ดวัน, พื้นหลังแผนที่, thumbnail) | `#F6F1EC` | `#22252B` |
| `--card3` | พื้นกล่องย่อยใน section | `#FDFAF7` | `#1D2026` |
| `--ink` | ข้อความหลัก, **ขอบและเงาทั้งหมด** | `#14161A` | `#F5F2EE` |
| `--ink2` | ข้อความรอง / คำอธิบาย | `#4A4F57` | `#B5B9BF` |
| `--ink3` | ข้อความระดับสาม / label | `#83888F` ⚠️ | `#868B93` |
| `--line` | เส้นขอบรอง (chip, ปุ่มรอง, เส้นประ) | `#E7DFD8` | `#2E3239` |
| `--line2` | เส้นแบ่งจาง ๆ ในรายการ | `#F1ECE6` | `#25282E` |
| `--or` | **สีแบรนด์ (ส้ม)** ปุ่มหลัก, สถานะ active, focus | `#F4622E` | `#FF7A48` |
| `--or-s` | พื้นส้มอ่อน (hover, แถวที่เลือก) | `#FFEADF` | `#3A1A0D` |
| `--or-d` | ส้มเข้มสำหรับ**ตัวอักษร** (ค่าใช้จ่าย) | `#BF4318` | `#FFA279` |
| `--hl` | ไฮไลต์ใต้คำ (ปากกาเน้นข้อความ) | `#FFC7A8` | `#6B3216` |
| `--teal` / `--teal-s` | สีรอง: สำเร็จ, เวลา, ความน่าเชื่อถือสูง, วันกลับ | `#17A79B` / `#DFF5F2` | `#3FCBBD` / `#0D2B28` |
| `--sky` / `--sky-s` | ข้อมูล / ลิงก์นำทาง / ความน่าเชื่อถือกลาง | `#2E7FB8` / `#E2EFF9` | `#6BB0DE` / `#13283A` |
| `--clay` / `--clay-s` | อันตราย, ลบ, ปิดทำการ | `#C0472B` / `#FAE3DD` | `#E4715A` / `#331812` |

- **โหมดมืด:** ทำงานผ่าน `prefers-color-scheme` และสลับเองได้ด้วย `data-theme="light|dark"` บน `<html>` (ปุ่ม ◐ บน header)
- **หมายเหตุ:** ในบล็อก `:root[data-theme="dark"]` ของต้นแบบ `--clay` ใช้ค่าของโหมดสว่าง (`#C0472B`) ซึ่งน่าจะเป็นความผิดพลาด ควรใช้ `#E4715A` ให้ตรงกับบล็อก `prefers-color-scheme`

### 2.2 สีหมวดสถานที่ (ใช้ทั้งหมุดบนแผนที่ จุดสีในรายการหมวด และ thumbnail)

| หมวด | สี | | หมวด | สี |
| --- | --- | --- | --- | --- |
| วัด · ศาสนสถาน (`temple`) | `#E8A11F` | | พิพิธภัณฑ์ · แหล่งเรียนรู้ (`museum`) | `#6A5FA8` |
| ทะเล · หาด · เกาะ (`sea`) | `#2E7FB8` | | ประวัติศาสตร์ · โบราณสถาน (`history`) | `#8A6432` |
| ภูเขา · น้ำตก · ถ้ำ (`mountain`) | `#0E5C55` | | ชุมชน · วิถีชีวิต (`local`) | `#96662F` |
| จุดชมวิว · ถ่ายรูป (`view`) | `#E8377D` | | ฟาร์ม · สวนเกษตร (`farm`) | `#7FA22E` |
| อุทยาน · ธรรมชาติ (`nature`) | `#3D8B4E` | | เด็กเล่น · สวนสนุก · สัตว์ (`kids`) | `#D6417A` |
| ตลาด · ของฝาก · ช้อปปิ้ง (`market`) | `#C0472B` | | สปา · นวด · สุขภาพ (`spa`) | `#149187` |
| อื่น ๆ (`other`) | `#87889A` | | | |

- แต่ละหมวดมีค่า**ความเหนื่อย** (`e`) ระดับ 0–2 ใช้จำกัดแผนให้เหมาะกับผู้สูงอายุหรือเด็ก
- ต้นแบบรวม `att_type` กว่า 70 ประเภทเป็น 13 หมวดนี้ ควรทำเป็น mapping ใน `src/lib/` หรือทำเป็น view ในฐานข้อมูล

### 2.3 สีแผนที่ (แผนที่วาดเองบน canvas)

| องค์ประกอบ | Light | Dark |
| --- | --- | --- |
| พื้นดิน | `#F4F1E9` | `#1B1F26` |
| ทะเล | `#AFD8E8` | `#0E1A24` |
| เส้นทางของผู้ใช้ | `--or` | `--or` |
| หมุดสถานที่ | สีตามหมวด (2.2) ขอบ `#0B0D10` | สีตามหมวด ขอบ `#FFFFFF` |

### 2.4 ความคมชัดของสี (WCAG) — ต้องแก้ก่อนนำไปใช้จริง

ต้นแบบมีหลายคู่สีที่ไม่ผ่านเกณฑ์ **4.5:1** สำหรับตัวอักษรขนาดปกติ

| คู่สีในต้นแบบ | Contrast | ใช้ที่ไหน | แนวทางแก้ |
| --- | --- | --- | --- |
| ขาว บน `--or` `#F4622E` | 3.17 | ปุ่ม `.cta`, chip ที่เลือก, วงกลมเลขขั้นตอน | พื้นปุ่มใช้ `#C8431A` (4.91) หรือใช้ตัวอักษร `--ink` บนส้ม (5.72) |
| `--or` บน `--card` | 3.17 | ข้อความส้ม เช่น `h1 em`, ตัวเลขในสถิติ | ถ้าเป็นข้อความเล็กใช้ `--or-d` `#BF4318` (5.21) ส่วน `--or` ใช้กับหัวข้อใหญ่ (≥ 24px) ได้ |
| `--ink3` `#83888F` บน `--paper` | 3.38 | label, คำอธิบายย่อย | เปลี่ยนเป็น `#6B7078` (4.72) |
| ขาว บน `--teal` `#17A79B` | 2.98 | chip `.t`, วันกลับในปฏิทิน, checkbox ที่ติ๊ก | พื้นใช้ `#0F766E` (5.47) |
| `--teal` บน `--teal-s` | 2.63 | badge `.b-hi`, `.sub` ที่เลือก | ตัวอักษรใช้ `#0F766E` (4.82) |
| `--sky` บน `--sky-s` | 3.71 | badge `.b-md`, `.nb` | ตัวอักษรใช้ `#1F5F8F` (5.81) |
| ขาว บน `--or` (โหมดมืด `#FF7A48`) | 2.58 | ปุ่มในโหมดมืด | โหมดมืดใช้ตัวอักษรดำบนส้ม (8.13) |

> โค้ดปัจจุบันใช้ `accent` เป็น `#C8431A` และ `secondary` เป็น `#0F766E` ซึ่งผ่านเกณฑ์แล้ว ([globals.css](src/app/globals.css)) ส่วน `--or` `#F4622E` ใช้ได้กับพื้นที่ที่ไม่มีตัวอักษรเท่านั้น เช่น เทปกาว เส้นทางบนแผนที่ หรือเงา active

### 2.5 ตัวอักษร

| บทบาท | ฟอนต์ | น้ำหนัก |
| --- | --- | --- |
| หัวข้อ (h1–h4, ชื่อแบรนด์, ค่าในปุ่มวันที่) | **Noto Serif Thai** | 600 / 700 / 800 |
| เนื้อหาและ UI | **Anuphan** | 300–700 (ปกติ 400, ปุ่ม 600–700) |
| ตัวเลข เวลา ค่าใช้จ่าย พิกัด | **IBM Plex Mono** + `tabular-nums` | 500 / 600 |

| ระดับ | ขนาด | หมายเหตุ |
| --- | --- | --- |
| Hero h1 | `clamp(26px, 4.2vw, 42px)`, 800, `letter-spacing: -.02em` | คำเน้นใช้สี `--or` และมีไฮไลต์ `--hl` สูง 9px ใต้คำ |
| Section h2 (`.sect`) | 21px | มีไฮไลต์ `--hl` สูง 8px ใต้ข้อความ |
| หัวขั้นตอน h3 | 15.5px | |
| หัวการ์ด h4 | 13.5–14px | |
| Body | 14px / line-height 1.6 | |
| Small | 12–12.5px | |
| Label (`label.fl`, `.grp > .t`) | 10.5–11px, 700, `letter-spacing: .07–.09em` | |

- หัวข้อทุกระดับใช้ `text-wrap: balance`
- **ข้อควรระวัง:**
  - ต้นแบบมีข้อความหลายจุดเล็กเพียง 9.5–11px (legend แผนที่, badge, label) ขณะที่ STYLE.md กำหนดว่าข้อความที่ต้องอ่านต้องไม่ต่ำกว่า 12px
  - ข้อความที่อ่านสำคัญควรใช้อย่างน้อย 12px และใช้ขนาดต่ำกว่านั้นเฉพาะ label ประกอบ
  - `text-transform: uppercase` และ `letter-spacing` ไม่มีผลดีกับภาษาไทย ใช้กับคำภาษาอังกฤษเท่านั้น

### 2.6 เลย์เอาต์และระยะห่าง

- **Container:** กว้างสูงสุด 1280px ขอบข้าง 20px บนจอใหญ่ และ 14px บนจอ ≤ 560px
- **ระยะห่างของ main:** บน 22px ล่าง 80px (เผื่อที่ให้แถบปุ่มด้านล่าง)
- **Breakpoints:**
  - 560px: header ย่อ และเมนูเลื่อนแนวนอน
  - 640px: grid 2–3 คอลัมน์กลายเป็นคอลัมน์เดียว
  - 680px: ปฏิทิน 2 เดือนเหลือ 1 เดือน
  - 760px: hero เปลี่ยนเป็นคอลัมน์เดียว
- **Grid ที่ใช้บ่อย:**
  - `.two` = 2 คอลัมน์ ห่าง 12px
  - `.three` = 3 คอลัมน์ ห่าง 10px
  - `.sug` = `auto-fill, minmax(228px, 1fr)` ห่าง 14px
  - `.stat` = `auto-fit, minmax(132px, 1fr)` ห่าง 10px
  - `.cols3` = `auto-fit, minmax(240px, 1fr)`

### 2.7 มุมโค้ง ขอบ และเงา

| Token | ค่า | ใช้กับ |
| --- | --- | --- |
| `--r` | 16px | การ์ดหลัก, hero, section, แผนที่ |
| การ์ดย่อย | 13px | การ์ดสถานที่, สถิติ, ตาราง |
| Control | 9–12px | input (10px), ปุ่มเล็ก (9px), ปุ่มวันที่ (12px) |
| Pill | 99px | chip, CTA, badge, เมนู |
| วงกลม | 50% | ปุ่ม +/−, ปุ่มสลับธีม, เลขขั้นตอน |
| ขอบหลัก | `2px solid var(--ink)` | ทุกอย่างที่เป็น "สติกเกอร์" |
| ขอบรอง | `1.5–2px solid var(--line)` | chip ที่ไม่ได้เลือก, ปุ่มรอง, input |
| เส้นแบ่งภายใน | `1.5–2px dashed var(--line)` | ระหว่างรายการ, หัวกับเนื้อของ section |
| `--hard` | `4px 4px 0 var(--ink)` (โหมดมืดใช้ `rgba(0,0,0,.6)`) | การ์ดหลักทั้งหมด |
| เงา active | `6px 6px 0 var(--or)` | section ที่เปิดอยู่ |
| เงาเล็ก | `2–3px 2–3px 0` | ปุ่มบนแผนที่, CTA, ปุ่มวันที่ที่เลือก |

**พื้นหลังกระดาษ:** จุดรัศมี 1px สี `--ink` ความทึบ 7% วางเป็นตาราง 14×14px
```css
background-image: radial-gradient(circle at 1px 1px, color-mix(in srgb, var(--ink) 7%, transparent) 1px, transparent 0);
background-size: 14px 14px;
```

### 2.8 Motion

- **Transition:** สั้น 0.13–0.15 วินาที ใช้กับ tooltip, toggle และลูกศรเปิด-ปิด
- **ตอนกดปุ่ม CTA:** ปุ่มเลื่อน `translate(1px, 1px)` และเงาลดจาก 3px เหลือ 2px ให้ความรู้สึกเหมือนกดสติกเกอร์ลงไป
- **ผู้ใช้ที่ตั้งลดการเคลื่อนไหว:** เมื่อ `prefers-reduced-motion: reduce` ต้องปิด animation และ transition ทั้งหมด

---

## 3. Components

ชื่อ class ในวงเล็บคือชื่อในต้นแบบ ถ้าสร้างเป็น React component ให้ใช้ชื่อไฟล์แบบ kebab-case ตาม STYLE.md

### โครงหน้า
- **Header (`header`, `.hin`)**
  - ติดด้านบน (sticky) พื้นใช้ `--paper` โปร่ง 93% + `backdrop-filter: blur(10px)` มีเส้นล่าง 2px สีหมึก
  - ประกอบด้วย brand mark → ชื่อ "ไทย**ไหนดี**" ("ไหนดี" สีส้ม ฟอนต์ Serif 800) → tagline 10.5px → เมนู pill → ปุ่มสลับธีม
  - จอ ≤ 560px ซ่อน tagline และย้ายเมนูลงบรรทัดใหม่แบบเลื่อนแนวนอน
- **Nav pills (`nav.vt button`)**
  - ปุ่ม pill สูงประมาณ 30px
  - หน้าที่อยู่ใช้ `aria-current="true"` พื้นหมึกกับตัวอักษรสีกระดาษ
  - hover แสดงขอบ `--line`
- **Hero (`.hero`)**
  - grid 1.25fr : 0.85fr แยกข้อความกับภาพ ภาพจางเข้าหาพื้นการ์ดด้วย gradient
  - ด้านบนมีป้าย eyebrow (`.eb`) เป็น pill ส้มตัวพิมพ์ใหญ่ เช่น "ข้อมูลเปิด ททท. · 8,361 แหล่งท่องเที่ยว"
  - `.sun` คือวงกลมตกแต่งมุมขวาบน ความทึบ 0.5
  - จอ ≤ 760px ภาพขึ้นไปอยู่ด้านบน
- **Section title (`.sect`)** หัวข้อ h2 มีไฮไลต์ใต้ข้อความ และคำอธิบายย่อยเป็น `.sm` วางบรรทัดเดียวกัน
- **Footer / ที่มาข้อมูล** บอกว่าข้อมูลส่วนไหนมาจาก ททท. และส่วนไหนระบบคำนวณเอง แบ่งเป็น 3 คอลัมน์ (`.cols3`) ต้องมีทุกหน้าที่แสดงผลลัพธ์

### พื้นผิว
- **Sticker (`.sticker`)** การ์ดพื้นฐาน ใช้พื้น `--card` ขอบหมึก 2px มุมโค้ง `--r` และเงา `--hard`
- **Tape (`.tape`)** แถบส้มขนาด 66×18px เอียง −3° ขอบหมึก 1.5px วางทับขอบบนการ์ด (`top: -9px`) ใช้แค่จุดเด่นหน้าละ 1–2 จุด
- **Warn / callout (`.warn`)** กล่องที่ขอบซ้ายหนา 7px มี 4 แบบ:

  | Variant | พื้น | ขอบซ้าย | ใช้เมื่อ |
  | --- | --- | --- | --- |
  | ปกติ | `--card2` | `--or` | ข้อสังเกตทั่วไป |
  | `.red` | `--clay-s` | `--clay` | ปิดทำการ, ความเสี่ยง |
  | `.k` | `--sky-s` | `--sky` | ข้อมูลอ้างอิง |
  | `.g` | `--teal-s` | `--teal` | ยืนยันแล้ว |

  หัวข้อในกล่องใช้ `<b>` แยกบรรทัด
- **Empty state (`.empty`)** จัดกลาง padding 48px สี `--ink3` และบอกทางออกเสมอ เช่น "ลองเปลี่ยนรูปแบบเส้นทางให้รัศมีกว้างขึ้น หรือเปิดหมวดเพิ่ม"

### ปุ่ม
| ชนิด | Class | รูปแบบ |
| --- | --- | --- |
| หลัก | `.cta` | pill พื้นส้ม ขอบหมึก เงา 3px ตัวหนา 14px (ดูการแก้คอนทราสต์ใน 2.4) |
| หลักสีเข้ม | `.cta.t`, `.cta.y` | พื้นหมึก ตัวอักษรสีกระดาษ |
| รอง | `.gbtn` | pill พื้นการ์ด ขอบ `--line` hover แล้วขอบเป็นหมึก |
| เล็กในรายการ | `.mini` | มุม 9px ขอบ `--line` · `.rm` = ลบ (ใช้ clay) · `.sw` = สลับ (พื้นส้มอ่อน) |
| เพิ่มรายการ | `.addbig` | กว้างเต็ม ขอบประ hover แล้วเป็นสีส้ม |
| เพิ่มลงแผน | `.addb` | pill พื้นหมึก เล็ก |
| วงกลม | `.stepper button`, `.calnav button`, `.tbtn` | 30–32px ขอบหมึก 2px |

### ฟอร์มและการเลือก
- **Input / Select** ขอบ `--line` 1.5px มุม 10px padding 7×11px เมื่อ focus ขอบเปลี่ยนเป็น `--or` label ใช้ `label.fl`
- **Chip (`.chip`)**
  - pill ขอบ `--line` 2px
  - เมื่อเลือก (`aria-pressed="true"`) มี 3 สี: หมึก (ค่าเริ่มต้นและ `.y`), ส้ม (`.p`), teal (`.t`)
  - จำนวนแสดงใน `.ct` ความทึบ 0.7
- **Stepper (`.stepper`)** แถวบอกจำนวน (ผู้ใหญ่ / เด็ก / ผู้สูงอายุ) มี label กับคำอธิบายย่อย ปุ่ม −/+ วงกลม และตัวเลขฟอนต์ mono ใช้เส้นประคั่นระหว่างแถว
- **Autocomplete (`.fwrap` + `.ac`)**
  - dropdown แบบสติกเกอร์ สูงสุด 300px เลื่อนได้
  - รายการที่ hover หรือถูกเลือกด้วยคีย์บอร์ด (`.on`) พื้นเป็น `--or-s`
  - มี `.sub` บอกอำเภอ/จังหวัด และแสดงผลของระดับจังหวัด → อำเภอ → ตำบล
- **ปุ่มเลือกวัน + ปฏิทินช่วงวัน (`.dbtn`, `.calwrap`)**
  - ปุ่มวันที่มี label ตัวเล็ก ค่าวันที่เป็นฟอนต์ Serif และชื่อวันในสัปดาห์ เมื่อเปิดอยู่ขอบส้มพร้อมเงาส้ม
  - ปฏิทินแสดง 2 เดือนคู่กัน วันเริ่มสีส้ม (`.s`) วันกลับสี teal (`.e`) วันระหว่างกลางพื้นส้มอ่อน (`.mid`) วันที่เลือกไม่ได้ความทึบ 0.25
- **กล่องหมวด (`.catbox`)**
  - หัวกล่องมีจุดสีหมวดขนาด 13px, ชื่อหมวด, จำนวนแห่ง และสวิตช์ `.tgl`
  - ด้านในเป็นปุ่มประเภทย่อย `.sub` ที่เลือกแล้วใช้พื้น teal อ่อน
- **Toggle (`.tg` 42×23, `.tgl` 36×20)** ขอบหมึก เมื่อเปิดพื้นเป็น teal ปุ่มวงกลมสีขาวเลื่อนไปทางขวา ใช้ `aria-pressed`
- **Tooltip (`.tip .tt`)**
  - กล่องพื้นหมึกกว้าง 252px มีหางชี้ แสดงเมื่อ hover หรือ focus-within
  - **ต้องมีทางแสดงบนจอสัมผัสด้วย** เพราะมือถือไม่มี hover

### ขั้นตอนวางแผน
- **Accordion step (`.sec`)**
  - หัวมี 3 ส่วน: วงกลมเลขขั้นตอน (ส้ม, mono), ชื่อขั้นตอนพร้อมสรุปค่าที่เลือก (`.sum`) และลูกศร ▾
  - ขั้นตอนที่เปิดอยู่ (`.on`) ได้เงาส้ม 6px และลูกศรหมุน 180°
  - เนื้อหาคั่นจากหัวด้วยเส้นประ แบ่งกลุ่มด้วย `.grp` ซึ่งมีหัวข้อกลุ่ม `.t`

### ผลลัพธ์
- **Stat tiles (`.stat .c`)** การ์ดสติกเกอร์เล็ก ค่าตัวเลข 23px ฟอนต์ mono และ label 11px ตัวเลขที่เป็นเงิน (ช่องที่ 5–6) ใช้สีส้ม
- **Map box (`.mapbox`)** กรอบสติกเกอร์ มีส่วนประกอบลอยบนแผนที่:
  - `.mtools` มุมซ้ายบน: แถบเครื่องมือ เช่น ลากเส้นทางเอง, เกาะถนน, ย้อนกลับ, ทำซ้ำ, เรียงจุด, ล้าง เครื่องมือที่เปิดอยู่พื้นส้ม
  - `.mzoom` มุมขวาบน: ปุ่ม + − และปรับให้พอดี
  - `.mleg` มุมซ้ายล่าง: สัญลักษณ์แผนที่ (`<details>`) พับเก็บได้
  - `.mhint` มุมขวาล่าง: คำแนะนำพื้นหมึก เช่น ชื่อถนนที่ใกล้ที่สุด
  - `.mhelp` ใต้แผนที่: กล่องวิธีใช้ ขอบประ พร้อมปุ่มลัด `<kbd>`
  - `.wpchip` ใต้แผนที่: pill ของจุดผ่าน มีเลขลำดับสีส้มและปุ่มลบวงกลม
- **Day card (`.day`)**
  - หัวการ์ด (`.dayh`) พื้น `--card2` มีป้าย "วันที่ n" (pill ส้ม), เมตา และค่าใช้จ่ายของวันสี `--or-d` ชิดขวา
  - แต่ละรายการ (`.rowitem`) เป็น grid 3 ส่วน: เวลา (mono สี teal, กว้าง 82px) | ชื่อ ประเภท และรายละเอียด | ปุ่ม `.mini`
- **ฟอร์มเพิ่มรายการ (`.form`)** ขอบส้ม 2px พื้น `--or-s` ผลค้นหา (`.sres`) เป็นรายการเลื่อนได้สูงสุด 200px
- **Suggestion card (`.sc`)**
  - thumbnail สัดส่วน 16:10 ใช้ภาพ SVG ที่สร้างอัตโนมัติ (ดู 5.3) ตามด้วยชื่อ คำอธิบาย และท้ายการ์ดเส้นประพร้อมปุ่ม "เพิ่ม"
  - สถานที่ที่ปิดในวันเดินทาง (`.shut`) ความทึบ 0.55
- **Badge (`.bdg`)** pill ตัวหนา 10.5px:

  | Class | ความหมาย | สี |
  | --- | --- | --- |
  | `.b-hi` | ข้อมูลน่าเชื่อถือสูง | teal |
  | `.b-md` | ปานกลาง | sky |
  | `.b-lo` | ต่ำ | ส้มเข้ม |
  | `.b-xx` | มีปัญหา / ปิด | clay |
  | `.b-sec` | เมืองรอง | พื้นส้ม ขอบหมึก |
  | `.b-main` | เมืองหลัก | sky |

- **ตาราง (`table.bk` ใน `.tw`)** กรอบสติกเกอร์ เลื่อนแนวนอนได้ (กว้างอย่างน้อย 620px) หัวตารางตัวเล็ก มีเส้นล่างหมึก 2px ระหว่างแถวใช้เส้นประ
- **ลิงก์นำทาง (`.nb`)** pill พื้น sky อ่อน ใช้กับลิงก์ออกไปภายนอก เช่น Google Maps, Wongnai, Lemon8
- **Checklist (`.ck`)** กล่องติ๊กขนาด 24px ขอบหมึก เมื่อติ๊กแล้วพื้นเป็น teal และข้อความถูกขีดฆ่า
- **Trip row (`.trip`)** การ์ดสติกเกอร์ 2 คอลัมน์ ข้อมูลทริปอยู่ซ้าย ปุ่มอยู่ขวา จอ ≤ 560px เรียงเป็นคอลัมน์เดียว
- **GPS readout (`.gps`)** แสดงพิกัดด้วยฟอนต์ mono 12px สี `--ink2`

---

## 4. โครงสร้างหน้า (Information Architecture)

เมนูหลักบน header มี 4 หน้า: **วางแผน** · **แผนของฉัน** · **กำลังเดินทาง** · **เข้าสู่ระบบ**

### 4.1 วางแผน (หน้าหลัก)
1. **Hero:** eyebrow "ข้อมูลเปิด ททท." พร้อมจำนวนแหล่ง → หัวข้อ "ไทยไหนดี — วางเส้นทางให้ตรงกับ*คนที่ไปด้วยจริง ๆ*" → คำอธิบาย → ภาพคอลลาจ
2. **ขั้นตอนแบบ accordion 5 ขั้น** เปิดทีละขั้น หัวของแต่ละขั้นสรุปค่าที่เลือกไว้:
   1. **จะไปไหน เมื่อไร**
      - ต้นทางและปลายทาง ค้นหาแบบ autocomplete ได้ระดับจังหวัด อำเภอ และตำบล และปักหมุดจาก GPS ได้
      - วันออกเดินทางและวันกลับ
   2. **เดินทางไปกับใคร**
      - จำนวนผู้ใหญ่ เด็ก และผู้สูงอายุ ใช้ stepper
      - ช่วงวัยของผู้ใหญ่เลือกได้หลายช่วง ระบบใช้ช่วงที่เดินไหวน้อยที่สุดเป็นเพดาน
   3. **ไปแนวไหน**
      - หมวดความสนใจ ใช้กล่องหมวด (`.catbox`) พร้อมประเภทย่อย
      - โอกาสของทริป และสวิตช์ "จัดแผนแบบสมดุลทุกวัย"
   4. **อยากแวะที่แบบไหน**
      - เลือกหมวดของจุดแวะ
      - บอกตรง ๆ ว่าร้านอาหาร ที่พัก และปั๊มน้ำมัน ไม่มีในข้อมูล ททท.
   5. **รถและค่าน้ำมัน**
      - ประเภท ยี่ห้อ รุ่น ขนาดเครื่องยนต์ และปีของรถ
      - ชนิดเชื้อเพลิง และราคาน้ำมัน (เป็นค่าตั้งต้น ต้องบอกผู้ใช้ว่าไม่ใช่ราคาสด)
3. **ผลลัพธ์ "ร่างแผนการเดินทาง"**
   - สถิติ: ระยะทางรวม, เวลาขับรวม, จำนวนจุดแวะ, ค่าน้ำมัน, ค่าใช้จ่ายรวม
   - แผนที่พร้อมเครื่องมือลากเส้นทาง
   - แผนรายวัน
   - สถานที่แนะนำ
4. **แถบปุ่ม (`.bar`)** มี CTA "สร้างแผนของฉัน" และปุ่มเปิดใน Google Maps
5. **Footer** อธิบายที่มาของข้อมูล

### 4.2 แผนของฉัน
- รายการทริปที่บันทึกไว้ แต่ละทริปมี badge นับถอยหลัง ("อีก n วัน" / "วันนี้")
- ปุ่ม "เริ่มแผน" พาเข้าโหมดนำทาง
- ถ้ายังไม่มีทริป แสดง empty state ที่บอกทางไปหน้าวางแผน

### 4.3 กำลังเดินทาง (Live)
- สวิตช์ "ติดตามตำแหน่งแบบต่อเนื่อง (GPS)" แสดงพิกัด ความแม่นยำ และเวลาอัปเดต
- บอกพื้นที่ที่ใกล้ที่สุดในฐานข้อมูล
- แผนของวันนี้ พร้อมปุ่มเปลี่ยนแผน ถ้าสถานที่ปิดหรือไปไม่ทัน
- ต้องบอกผู้ใช้ว่าตำแหน่งไม่ถูกส่งออกไปที่ไหน

### 4.4 เข้าสู่ระบบ
- โลโก้ (`.authlogo`) และหัวข้อ "เก็บ*แผนเที่ยว*ของคุณไว้ กลับมาใช้ได้ทุกเมื่อ"
- ปุ่ม Facebook, Google และสมัครด้วยอีเมล
- ในต้นแบบเป็นโหมดสาธิต ของจริงใช้ Supabase Auth ที่มีอยู่แล้ว

---

## 5. ภาพและไอคอน

### 5.1 มาสคอตและโลโก้
- **มาสคอต:** ชายสวมหมวกแก๊ปสีส้มกลับหลัง จับพวงมาลัย ใบหน้าเป็นลายเส้นแบบหนังตะลุงหรือโขน กลางพวงมาลัยมีหมุดตำแหน่งสีส้ม
- **Wordmark:** "ไทย" สีหมึก "ไหนดี" สีส้ม ใต้คำมี `thainhaidee.com` ขนาบด้วยเส้นสีเทอร์ควอยซ์
- **ห้าม:** เปลี่ยนสีมาสคอต ตัดหมวกออก หรือวางโลโก้บนพื้นลายที่ทำให้ขอบสติกเกอร์สีขาวหายไป

### 5.2 ภาพ Hero
- สไตล์คอลลาจตัดแปะ ขอบขาวแบบสติกเกอร์ ท้องฟ้าเทอร์ควอยซ์ พระอาทิตย์เหลืองและชมพู
- องค์ประกอบ: สถานที่สำคัญของไทย (วัด เจดีย์ เสาชิงช้า เกาะหินปูน เรือหางยาว), รถมินิสีส้ม, ใบไม้เขตร้อน
- ภาพใหม่ที่เพิ่มเข้ามาควรใช้โทนและเทคนิคเดียวกันนี้

### 5.3 Thumbnail ที่สร้างอัตโนมัติ
ข้อมูล ททท. ไม่มีรูป ต้นแบบจึงสร้างภาพ SVG เรียบ ๆ ตามหมวด โดยใช้สีหมวด (2.2) และสุ่มรูปทรงจาก hash ของชื่อ ภาพเดียวกันจะได้ทุกครั้ง
- ทะเลและสปา: คลื่น 3 ชั้น
- ภูเขา ธรรมชาติ และจุดชมวิว: ภูเขา 2 ลูก (จุดชมวิวมีดวงอาทิตย์เพิ่ม)
- วัดและประวัติศาสตร์: ยอดเจดีย์กับอาคาร

ต้องมีหมายเหตุว่า "ภาพประกอบตามหมวด ไม่ใช่ภาพจริง"

### 5.4 ไอคอน
- ต้นแบบใช้ตัวอักษรหรือ emoji แทนไอคอน เช่น ◐ ▾ ⛽ 🅿️ ซึ่งแสดงผลต่างกันในแต่ละเครื่อง
- ของจริงให้ใช้ชุดไอคอน SVG ชุดเดียวทั้งเว็บ เช่น Lucide หรือ Phosphor ที่ stroke 2px ให้เข้ากับเส้นขอบ
- ปุ่มที่มีแต่ไอคอนต้องมี `aria-label` ทุกครั้ง

---

## 6. น้ำเสียงและข้อความ

- **ถามแบบบทสนทนา:** "จะไปไหน เมื่อไร", "เดินทางไปกับใคร", "ไปแนวไหน", "อยากแวะที่แบบไหน"
- **หัวข้อสรุปในแต่ละขั้นตอนบอกค่าที่เลือกไว้เสมอ** เช่น "2 คน · ผู้ใหญ่ 2" หรือ "ยังไม่เลือกแนว"
- **บอกข้อจำกัดตรง ๆ โดยใช้กล่อง `.warn` ไม่ซ่อนไว้ใน footnote** เช่น "ราคาน้ำมันเป็นค่าตั้งต้น ไม่ใช่ราคาสด" หรือ "ระยะทางเป็นเส้นตรง"
- **ตัวเลข:**
  - ใช้ฟอนต์ mono และมีหน่วยภาษาไทยเสมอ (กม., นาที, บาท, ลิตร)
  - วันที่ใช้ `th-TH` และ timezone `Asia/Bangkok` ตาม STYLE.md
- **คะแนนข้างชื่อสถานที่คือความน่าเชื่อถือของข้อมูล ไม่ใช่คะแนนรีวิว** ต้องอธิบายไว้ใน footer

---

## 7. Accessibility

สิ่งที่ต้นแบบทำดีแล้ว และต้องคงไว้:
- **Focus:** `:focus-visible` เป็นเส้นส้ม 2.5px ห่างจากขอบ 2px
- **ลดการเคลื่อนไหว:** เคารพ `prefers-reduced-motion`
- **สถานะผ่าน ARIA:** ใช้ `aria-pressed`, `aria-current` และ `aria-expanded` บอกสถานะ ไม่ได้อาศัยแค่สี
- **ปุ่มสลับธีม** มี `aria-label`

สิ่งที่ต้องแก้ตอนนำไปใช้จริง:
1. **ความคมชัดของสีตามตาราง 2.4**
2. **ขนาดเป้าที่กด:**
   - ต้นแบบมีปุ่มเล็ก เช่น ปุ่ม +/− 30px, ปุ่มลบจุดผ่าน 18px, ปุ่มวันที่ในปฏิทิน 34px
   - บนจอสัมผัสควรมีพื้นที่กดอย่างน้อย 44×44px (ขยายพื้นที่กดด้วย padding หรือ pseudo-element ได้)
3. **Tooltip** ที่แสดงเฉพาะตอน hover ต้องมีทางเปิดด้วยการแตะหรือกดคีย์บอร์ดด้วย
4. **ขนาดตัวอักษร:** ข้อความที่ต้องอ่านต้องไม่ต่ำกว่า 12px (ดู 2.5)
5. **แผนที่บน canvas:** ต้องมีรายการจุดแบบข้อความให้ screen reader อ่านได้ เช่น แผนรายวันหรือ `.wpchip` และมีปุ่มลัดคีย์บอร์ดตามที่ `.mhelp` อธิบายไว้

---

## 8. การนำไปใช้กับโค้ด Next.js ปัจจุบัน

### 8.1 จับคู่ token

| ต้นแบบ | `globals.css` ปัจจุบัน | สิ่งที่ต้องทำ |
| --- | --- | --- |
| `--paper` `#FDF8F4` | `--background` `#faf7f2` | ปรับให้ตรงค่าต้นแบบ และเพิ่มลายจุดที่ `body` |
| `--card` | `--surface` | ค่าตรงกันแล้ว |
| `--card2`, `--card3` | ยังไม่มี | เพิ่มเป็น `surface-2` และ `surface-3` |
| `--ink` | `--foreground` | ค่าใกล้เคียงกัน (`#14161A` / `#1c1c1a`) |
| `--ink2` / `--ink3` | `--muted` | แยกเป็น `muted` (`#4A4F57`) และ `subtle` (`#6B7078` ค่าที่แก้คอนทราสต์แล้ว) |
| `--line` / `--line2` | `--border` | เพิ่ม `border-soft` |
| `--or` / `--or-d` / `--or-s` | `--accent` `#C8431A` | คง `accent` ไว้เป็นสีส้มที่ใช้กับตัวอักษรได้ เพิ่ม `brand` (`#F4622E` สำหรับพื้นที่ไม่มีตัวอักษร) และ `accent-soft` |
| `--teal` / `--teal-s` | `--secondary` `#0F766E` | เพิ่ม `secondary-soft` |
| `--sky`, `--clay`, `--hl` | `--danger` ใกล้เคียง clay | เพิ่ม `info`, `info-soft`, `highlight` |
| `--hard`, `--r` | ยังไม่มี | เพิ่ม `--shadow-hard` และ `--radius-card` ใน `@theme` |

ตัวอย่างสำหรับ Tailwind v4 (`src/app/globals.css`):

```css
@theme inline {
  --shadow-hard: 4px 4px 0 var(--foreground);
  --shadow-hard-sm: 2px 2px 0 var(--foreground);
  --shadow-active: 6px 6px 0 var(--brand);
  --radius-card: 16px;
  --font-display: var(--font-noto-serif-thai), Georgia, serif;
  --font-sans: var(--font-anuphan), ui-sans-serif, system-ui, sans-serif;
  --font-mono: var(--font-plex-mono), ui-monospace, monospace;
}
```

ใช้งานใน JSX แบบนี้: `rounded-card border-2 border-foreground bg-surface shadow-hard`

### 8.2 ฟอนต์
- ตอนนี้ใช้ Noto Sans Thai เป็นฟอนต์หลัก ให้เปลี่ยนเป็น **Anuphan** (เนื้อหา), **Noto Serif Thai** (หัวข้อ) และ **IBM Plex Mono** (ตัวเลข) โหลดผ่าน `next/font/google` ทั้งหมด
- เมื่อเปลี่ยนแล้วต้องอัปเดตข้อ 3.2 ใน STYLE.md ให้ตรงกันด้วย

### 8.3 ฟีเจอร์ในต้นแบบที่ยังไม่มีในเว็บ
- ขั้นตอนวางแผน 5 ขั้นและการสร้างแผนรายวันอัตโนมัติ (ต้นแบบมีฟังก์ชัน `autoPick` และ `buildSchedule`)
- หน้าแผนของฉัน ต้องมีตารางบันทึกทริปที่ผูกกับ `auth.users` และตั้ง RLS ให้แต่ละคนเห็นเฉพาะทริปของตัวเอง
- โหมดกำลังเดินทาง ต่อยอดจาก hook `useGeolocation` ที่มีอยู่แล้ว
- การรวมประเภทสถานที่เป็น 13 หมวด พร้อมค่าความเหนื่อย
- ค่าน้ำมันจากข้อมูลรถ

### 8.4 สิ่งที่เว็บมีแล้ว แต่ต้นแบบทำต่างออกไป
- **แผนที่:**
  - ต้นแบบวาดแผนที่เองบน canvas
  - เว็บใช้ Leaflet ร่วมกับ OSM (หน้า `/map`) และ Google Maps embed
  - ให้คงไลบรารีเดิมไว้ แล้วปรับหน้าตาให้เป็นแบบสติกเกอร์ตาม 2.3 และ 3 (แผนที่)
- **ปั๊มน้ำมันและจุดพักรถ:** ต้นแบบบอกว่า "ไม่มีข้อมูล" แต่เว็บมีตาราง `roadside_poi` จาก OSM แล้ว ให้แสดงพร้อมบอกว่าเป็นข้อมูลจาก OSM
- **จำนวนข้อมูล:** ต้นแบบนับได้ 8,361 แห่ง เพราะคัดระเบียนทดสอบและระเบียนที่พิกัดขัดแย้งออก 45 แห่ง ส่วนฐานข้อมูลตอนนี้มีประมาณ 8,600 แห่ง ควรคัดออกด้วยเกณฑ์เดียวกันก่อนแสดงจำนวนบนหน้าเว็บ
